# Dependencies
node_modules/
package-lock.json

# Rust
target/
src-tauri/target/

# Tauri generated mobile projects (regenerated by `cargo tauri ios/android init`)
src-tauri/gen/

# Cargo lock is tracked (don't ignore)
# Tauri gen/schemas is tracked (don't ignore)
.idea
